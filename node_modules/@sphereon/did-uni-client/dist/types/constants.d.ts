import { DIDResolutionResult } from 'did-resolver';
import { Config } from './types';
export declare const Constants: {
    INVALID_DID: string;
    URL_PATHNAME_REGEX: RegExp;
};
export declare const DefaultURLs: {
    /** Registrar URL's*/
    CREATE: string;
    UPDATE: string;
    DEACTIVATE: string;
    /** Resolver URL's*/
    RESOLVE: string;
};
export declare const DefaultConfig: Config;
export declare const errorResolutionResult: (error: string) => Promise<DIDResolutionResult>;
