import { ISimpleLogger, SimpleLogOptions } from '@sphereon/ssi-types';
import { EventLoggerArgs, LoggingEvent } from '../../types';
declare class EventLogger {
    private readonly context?;
    private readonly namespace;
    private readonly system?;
    private readonly subSystemType?;
    private readonly logLevel;
    private readonly initiatorType?;
    private static readonly LOGGERS;
    constructor(args: EventLoggerArgs);
    private localListener;
    simple: (options?: Omit<SimpleLogOptions, 'namespace'>) => ISimpleLogger<unknown>;
    logEvent: (event: LoggingEvent) => Promise<void>;
    private eventData;
}
export default EventLogger;
//# sourceMappingURL=EventLogger.d.ts.map